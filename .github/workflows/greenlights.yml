name: Greenlights

on:
  schedule:
    - cron: '37 5 * * *'
  workflow_dispatch:

jobs:
  greenlights:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repo
        uses: actions/checkout@v4

      - name: Configurar git
        run: |
          git config user.name "Julio Reyes"
          git config user.email "juliojesusreyes@gmail.com"

      - name: Generar commits
        run: |
          # Si es lunes, reiniciar el archivo y hacer un commit especial
          if [ "$(date +%u)" -eq 1 ]; then
            echo "# Week of $(date +%F)" > .upflow.txt
            git add .upflow.txt
            git commit -m "New greenlights 🌱 - $(date +%F)"
          fi

          COMMITS=$(( ( RANDOM % 5 )  + 1 ))
          for i in $(seq 1 $COMMITS); do
            echo "Commit $i - $(date)" >> .upflow.txt
            git add .upflow.txt
            git commit -m "Green light $i - $(date +%F_%T)"
          done

      - name: Token Push
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git remote set-url origin https://x-access-token:${TOKEN}@github.com/${{ github.repository }}
          git push
